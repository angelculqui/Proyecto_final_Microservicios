server:
  port: 8081

spring:
  application:
    name: customer-service

  data:
    mongodb:
      uri: mongodb://mongo:27017/bank_db  # 🔥 clave para Docker

logging:
  level:
    root: INFO
    com.bank.customer_service: DEBUG